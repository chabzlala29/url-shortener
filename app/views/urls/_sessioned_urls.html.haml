- if @sessioned_urls.present?
  .sessioned-urls-wrapper
    %h5.session-head Recently generated short URLs:
    %p.session-head Click to see full information

    .list-group
      - @sessioned_urls.each do |url|
        .list-group-item.list-group-item-action.flex-column.align-items-start
          .d-flex.w-100.justify-content-between
            %h5.mb-1= full_shorten_url(url)
            %small= distance_of_time_in_words(url.last_visited_at, Time.zone.now)
          %small
            Original URL:
            %span= link_to url.original_url, url.original_url, target: :_blank
          %p
            %small= link_to "more details", "/data/#{url.shorten_url}"
